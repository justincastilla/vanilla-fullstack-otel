# This is an example of a .env file for the project.
# Copy this file to .env and fill in the required values.
# IMPORTANT: Do not use quotes around the values!

# Elastic APM Configuration
# This is the URL of your Elastic instance APM endpoint.
ELASTIC_ENDPOINT=https://your-deployment.apm.us-central1.gcp.elastic-cloud.com:443
# This is the token for your Elastic instance. You can find this in the Observability dashboard.
# IMPORTANT: Format must be "ApiKey <your-token>" not "Bearer <your-token>"
ELASTIC_TOKEN=ApiKey your-token-here

# Weather API Configuration
# API key for the weather API demo (get from https://www.weatherapi.com/)
WEATHER_API_KEY=your-weather-api-key

# Elasticsearch Cache Configuration (Optional - for weather caching demo)
# Elasticsearch endpoint URL (include https:// and port if needed)
ELASTICSEARCH_ENDPOINT=https://your-deployment.kb.us-west1.gcp.elastic.cloud:443
# Elasticsearch API key (base64 encoded id:api_key)
ELASTICSEARCH_API=your-base64-encoded-api-key
# Index name for weather cache
CACHE_INDEX=weather-cache